<%= bootstrap_form_with(model: pad, local: true) do |form| %>
  <% if pad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pad.errors.count, "error") %> prohibited this pad from being saved:</h2>

      <ul>
      <% pad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= form.text_field :name %>
    <%= form.text_field :description %>
    <%= form.hidden_field :created_by_id, value: current_user.id %>
    <%= form.submit %>
  </div>
<% end %>
